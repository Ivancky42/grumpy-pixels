# Grumpy Pixels — SEO Playbook

Target market: Malaysian SMEs and organizations that need a new or better website.
Niche: software + design, Malaysia. The strategy is **intent-driven keywords**, not
broad head terms.

---

## 1. Keyword map

### Core service keywords (sitewide + home)

- web design studio malaysia (brand-adjacent, used naturally in copy)
- nextjs development malaysia
- website redesign malaysia / website revamp malaysia
- website speed optimization malaysia
- technical seo malaysia
- shopify developer malaysia

Avoid competing head-on for "web design malaysia" / "website company malaysia" —
dominated and low-intent. We rank for specific problems instead.

### Pricing keywords (`/pricing`)

- how much does a website cost in malaysia (primary, H1)
- website design cost malaysia / website development cost malaysia
- corporate website cost malaysia
- website redesign malaysia, website speed optimization malaysia (reinforced)
- nextjs development malaysia (reinforced)

The pricing page is written as an educational budget guide ("educate first,
sell second") so it can rank for cost-intent queries and be quoted by AI
search engines.

### Industry keywords (one landing page each)

| Page | Primary keyword | Secondary |
| --- | --- | --- |
| `/web-design/clinics` | clinic website design malaysia | dental clinic website, aesthetic clinic web design, doctor website malaysia |
| `/web-design/restaurants` | restaurant website design malaysia | cafe website design, food menu website, restaurant web designer |
| `/web-design/law-firms` | law firm website design malaysia | lawyer website design, legal practice website |
| `/web-design/property-agencies` | property agency website design malaysia | real estate agent website, property listing website malaysia |
| `/web-design/gyms` | gym website design malaysia | fitness studio website, personal trainer website |
| `/web-design/tuition-centres` | tuition centre website design malaysia | education centre website, enrichment centre website malaysia |

These industries are chosen because Malaysian businesses in them actively buy
websites: clinics (MOH-regulated info, bookings, trust), F&B (menus, Google Maps
traffic), law firms (credibility), property agents (listings, lead gen), fitness
(class schedules, sign-ups), tuition centres (enrolment, parent trust).

## 2. URL and information architecture

```
/                          home (services, work, industries, process, contact)
/work                      case studies (MAPACS, PersonEdge)
/pricing                   budget guide ("how much does a website cost in malaysia")
/web-design/[industry]     6 static industry landing pages
/llms.txt                  static AI-crawler summary (public/llms.txt)
/sitemap.xml  /robots.txt  generated by app/sitemap.ts and app/robots.ts
```

- Flat, crawlable, every page ≤ 1 click from home.
- Footer links to all industry pages on every page (internal linking).
- Industry pages cross-link to /work and home contact section.

## 3. Metadata patterns

Defined via the Next.js Metadata API:

- Root layout: `title.template = "%s | Grumpy Pixels"`, default title
  `"Grumpy Pixels — Web Design & Development Studio in Malaysia"`.
- `metadataBase` set from `lib/site.ts` `siteUrl` so OG/canonical URLs resolve.
- Each page sets `title`, `description` (≤ 160 chars, includes primary keyword +
  Malaysia), and `alternates.canonical`.
- Industry pages generate metadata from `lib/industries.ts` data.
- OG image: static `app/opengraph-image.png` (1200x630) applies sitewide.

Title formula for industry pages:

> `{Industry} Website Design Malaysia` → e.g. "Clinic Website Design Malaysia"

## 4. Structured data (JSON-LD)

- Sitewide (root layout): `ProfessionalService` — name, url, logo, areaServed
  Malaysia, contact email/WhatsApp, `priceRange`, `knowsAbout` service list.
- Industry pages: `Service` (serviceType + provider) and `FAQPage` built from each
  industry's FAQ entries in `lib/industries.ts`.
- `/work`: `CreativeWork` items naming the client projects.
- `/pricing`: `Service` with `Offer`s (`PriceSpecification` in MYR per tier from
  `lib/pricing.ts`) and `FAQPage` from the pricing FAQ.

Rendered via the `JsonLd` component (server-side `<script type="application/ld+json">`).

## 5. Technical SEO checklist

- [x] One `h1` per page containing the primary keyword
- [x] Semantic landmarks: `header`, `main`, `footer`, `nav`, `section` + headings hierarchy
- [x] `app/sitemap.ts` lists all static + industry routes
- [x] `app/robots.ts` allows all, points to sitemap
- [x] Descriptive `alt` on all images
- [x] Self-hosted fonts (`next/font`), no render-blocking third parties
- [x] Core Web Vitals: static rendering, `next/image`, priority on hero image only
- [x] Mobile-first responsive — Google indexes mobile

## 6. AI search (GEO/AEO)

Optimizations so the site surfaces in AI-based search (ChatGPT, Perplexity,
Google AI Overviews, Claude):

- **`public/llms.txt`** — plain-language summary of services, pricing ranges,
  industries, work and contact info, following the llms.txt convention. Keep it
  in sync when pricing or services change.
- **`app/robots.ts`** explicitly allows AI crawlers (GPTBot, OAI-SearchBot,
  ClaudeBot, PerplexityBot, Google-Extended, etc.) in addition to `*`.
- **Quotable, answer-shaped content**: the pricing page leads with a direct
  question H1 and gives concrete RM figures in the first screen; FAQs answer
  questions in 1–3 self-contained sentences so engines can lift them verbatim.
- **Structured data with real prices** (`Offer`/`PriceSpecification` in MYR)
  gives engines machine-readable facts to cite.
- **Entity consistency**: name, location (Malaysia), services and price range
  are stated identically across JSON-LD, llms.txt and visible copy.

## 7. Copy guidelines for SEO

- Mention "Malaysia" naturally (location line, copy) — never keyword-stuff.
- Industry pages must have unique, genuinely useful copy: pain points, what we
  build, FAQ. No find-and-replace boilerplate across industries.
- Outcome language ("more bookings", "more walk-ins") beats feature language.

## 8. Future content ideas (not built yet)

- Case study deep-dives with before/after Core Web Vitals numbers
- "How fast is your website?" interactive audit lead magnet
- Blog targeting long-tail: "shopify vs custom website malaysia",
  "why is my wordpress site slow"
  (note: "how much does a website cost in malaysia" is now covered by /pricing)
